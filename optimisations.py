# remove "class Solution" and unindent

# add the following lines of code

sys.stdout=open('user.out','w')
for i in map(loads,stdin):
    print(FUNCTION_NAME_HERE(i))
exit()

# you may need to edit "i" to multiple variables if the question requires it